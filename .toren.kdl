setup {
    // template src=".env.breq" dest=".env"
    share src=".beads"
    copy src="target"
    copy src="web/node_modules"
    // run "pnpm install" cwd="web"
    // run "just migrate"
}

destroy { }

