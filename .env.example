# Required: Anthropic API Key
ANTHROPIC_API_KEY=your_api_key_here

# Required for testing: Fixed pairing token (defaults to random if not set)
# Daemon will use this token, and `just prompt` will auto-pair on first use
PAIRING_TOKEN=123456

# Optional: Session Token (auto-generated on first `just prompt`)
# If set, skips auto-pairing. Sessions persist in ~/.toren/sessions.json
# SESSION_TOKEN=your_session_token_here

# Optional: Ship URL (defaults to localhost)
# SHIP_URL=ws://localhost:8787
