# Toren test configuration
# Used by `just daemon` and `just cli` for local development/testing
# Uses a separate port and workspace dir to avoid conflicting with a real install

[server]
host = "127.0.0.1"
port = 8788

[ancillaries]
segments = ["examples"]
workspace_root = "/tmp/toren-ws"
